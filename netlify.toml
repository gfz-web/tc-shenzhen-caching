[build]
publish = "dist"
command = "pnpm run build"

[build.environment]
NODE_VERSION = "20"
# Supabase: set NUXT_SUPABASE_URL + NUXT_SUPABASE_ANON_KEY (and/or NUXT_PUBLIC_*) in Netlify → Environment variables.
